// In later commits  will fill array with real cities for autocomplete and validation

"use strict";

/**
 * Global city catalog
 * Each entry will have the shape:
 * { id: string, name: string, country: string, lat: number, lon: number }
 *
 * For now empty on purpose – only priparing structure
 */
var CITY_CATALOG = [];

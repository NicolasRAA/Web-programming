# ЛР3 — 2048

> [!IMPORTANT]
> Работа выполнена **на чистом JavaScript (DOM API)**, без фреймворков и без `innerHTML`/`outerHTML`;
> **Ячейки поля 4×4 и все тайлы создаются динамически** через JS (`createElement`, `appendChild`, `textContent`, `className`);


## Оглавление
1. [Кратко о работе](#кратко-о-работе)  
2. [Страница](#страница)  
3. [Что требовалось по заданию](#что-требовалось-по-заданию)  
4. [Функционал](#функционал)  
5. [Технологии](#технологии)

---

## Кратко о работе
Веб-версия игры **2048**: движения во все стороны, каскадные слияния, подсчёт очков за ход, **Undo**, **Leaderboard (Top-10)**, сохранение прогресса в `localStorage`.   
Поддерживаются **клавиши со стрелками (для desktopа)** и **виртуальные кнопки (mobile)**, есть простые анимации.

---

## Страница
- Главная страница работ: **https://nicolasraa.github.io/Web-programming/**
- ЛР3 (2048): **https://nicolasraa.github.io/Web-programming/lab-3/**

---

## Что требовалось по заданию
- Базовая страница: заголовок, контейнер для игрового поля, счёт, модалка/страница для лидерборда.
- **Поле 4×4** и тайлы создаются **динамически** через JS.
- При **окончании игры**: модалка с вводом имени, кнопка «сохранить рекорд», «начать заново».
- **Таблица рекордов**: имя, количество очков, дата; **Top-10**.
- Адаптивность: корректное отображение на ноутбуках/планшетах/смартфонах.  
  - Desktop — управление стрелками.  
  - Mobile — виртуальные кнопки или swipes.
> [!WARNING]
> Запрет на фреймворки; допускается SCSS.
> Запрет на `innerHTML`/`outerHTML`.

---

## Функционал
- **Генерация поля 4×4** и тайлов целиком через DOM API.  
- **Старт**: 1~3 случайных тайла со значением **2/4**.  
- **После каждого хода**: появляются 1~2 новых тайла **2/4** в случайных свободных ячейках.  
- **Слияние** одинаковых чисел работает во всех направлениях; очки считаются как сумма значений объединённых тайлов за ход.
- **Проверка конца игры**, т.е. проверка, что нет возможных ходов и **Game Over** модальное окно.
- **Undo** всешо на одного хода, и после окончания игры — недоступно.
- **Restart** без перезагрузки страницы.
- **Leaderboard** (Top-10, `localStorage`) — имя/очки/дата, для первых 3 мест отображаются 🥇🥈🥉.
- **Сохранение сессии**: поле, счёт, состояние игры — в `localStorage`; восстановление при загрузке.
- **Управление**: стрелки на клавиатуре (desktop) + виртуальные кнопки (mobile).
- **UI**: бейджи Score/Record, простой хинт про клавиатуру (desktop), плавные анимации появления/движения/слияния.

---

## Технологии
- **JavaScript** (DOM API, события, `localStorage`)
- **CSS** (адаптивные стили, тёмная тема; без фреймворков)
- **HTML5** (файл с подключением скрипта)

> Деплой выполнен с помощью **GitHub Pages**.

